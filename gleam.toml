name = "gleamcord_http"
version = "0.0.1"

target = "erlang"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
description = "A library to quickly and easily make Discord bots using the Discord HTTP API"
# licences = ["Apache-2.0"]
repository = { type = "github", user = "H-274", repo = "gleamcord_http" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

internal_modules = ["gleamcord_http/internal"]

gleam = ">= 1.13.0"

[dependencies]
gleam_stdlib = ">= 0.60.0 and < 1.0.0"
wisp = ">= 2.0.0 and < 3.0.0"


[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
asset = ">= 1.2.0 and < 2.0.0"
mist = ">= 5.0.3 and < 6.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"
